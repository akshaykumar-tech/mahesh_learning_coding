<%= form_with(model: akshay) do |form| %>
  <% if akshay.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(akshay.errors.count, "error") %> prohibited this akshay from being saved:</h2>

      <ul>
        <% akshay.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
